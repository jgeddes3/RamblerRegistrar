// theme.js — shared visual constants (P1 typography sweep).
// The app's brand font is loaded once in App.js (useFonts). Newer screens
// (Phases 2–4) shipped on the system font; import FONT here instead of
// hardcoding the family string so future font changes are one-line.
export const FONT = 'CormorantGaramond-Regular';
export const MAROON = '#A30046';
